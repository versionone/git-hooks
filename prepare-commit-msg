#!/usr/bin/env ruby

require File.expand_path('../git_wrapper', __FILE__)

commit = GitCommit.new
asset = commit.asset_for_branch

exit unless asset
exit if commit.message.include? asset

File.open(ARGV[0], 'w') do |f|
  f.puts commit.message.strip
  f.puts ''
  f.puts ''
  f.puts "VersionOne: #{asset}"
  f.puts commit.extra_comments
end

